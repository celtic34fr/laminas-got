<?php

function setEvents($events) {
    if (!empty($events)) {
        foreach ($events as $evt => $event) {
            $str .= 'data-' . $evt . '-stopevt = "' . $event['stopEvent'] . '" ';
        }
    }
    return $str;
}
